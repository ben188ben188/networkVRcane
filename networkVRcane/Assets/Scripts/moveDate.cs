using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class moveDate : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        print("位置坐标" + this.transform.localPosition);

        print("X：" + this.transform.localPosition.x);

        print("Y：" + this.transform.localPosition.y);

        print("Z：" + this.transform.localPosition.z);
        //Users/yangbin/Desktop/research/data/data1.txt
        

    }
}
